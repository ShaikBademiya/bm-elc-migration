.git
.github

.venv
.vscode

__pycache__
*.pyc

build

.env

# google-github-actions/auth writes the resolved credential to a file in the
# workspace. It must never be committed, and must never be copied into an image.
gha-creds-*.json
